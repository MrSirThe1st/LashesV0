import { DefaultTheme } from "react-native-paper";

const customTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#1e90ff", // Set the primary color to #1e90ff (blue)
  },
};

export default customTheme;
